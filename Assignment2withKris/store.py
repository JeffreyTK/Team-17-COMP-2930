

class Store:

    # must receive orders and maintain the inventory
    # must get items from factory class if not enough in stock, create 100 if none exist
    # generate a daily transaction report
    pass
